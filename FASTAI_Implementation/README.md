## FASTAI implementation for foreground segmentation.

Training procedure:
1. Develop a folder containing the labels (mask in the segmentation task) and the images.
2. Modify the path and the regex to map the labels with the particular image.
3. Train and predict.

